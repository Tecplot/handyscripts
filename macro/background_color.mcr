#!MC 1410
# Use this script to remove the gray line between two frames before exporting an image
$!Interface ScrBackgroundColor = White    #(or a custom color to match your plot)
$!Redraw #redraws the plot and recolors the background
